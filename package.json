{
  "name": "intelligent-task-routing",
  "version": "2.0.0",
  "description": "AI-powered intelligent task routing and prioritization for Jira",
  "main": "src/index.js",
  "scripts": {
    "build": "npm run build:triage",
    "build:admin": "echo 'Admin UI uses static files, no build needed'",
    "build:triage": "cd static/triage-dashboard && npm run build",
    "deploy": "forge deploy",
    "install-app": "forge install",
    "logs": "forge logs",
    "tunnel": "forge tunnel",
    "setup": "cd static/triage-dashboard && npm install"
  },
  "dependencies": {
    "@forge/api": "^4.0.0",
    "@forge/resolver": "^1.8.0",
    "openai": "^4.28.0"
  },
  "devDependencies": {
    "@forge/cli": "^12.0.0"
  },
  "keywords": [
    "jira",
    "ai",
    "task-routing",
    "automation",
    "forge"
  ],
  "author": "AI Task Routing Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
